---
id: task-2026-07-18-spec-0003-impl
type: task
title: "Implement spec-0003: SignatureFactory and Vocabulary types"
status: done
version: 0.1.0
owner: hugo
created: 2026-07-18
updated: 2026-07-18
milestone: M1
related: [spec-0003]
impl_progress: 100
tags: [m1, implementation, ts]
---

# Tasks for spec-0003 implementation — 2026-07-18

This task implements Implementation Spec spec-0003 (SignatureFactory and Domain Types). The spec is the contract; this file is the day plan.

## Sub-tasks

- [x] T1 Create `packages/vocabulary/` skeleton (package.json, tsconfig.json, tsconfig.build.json, vitest.config.ts)
- [x] T2 Implement `src/domain/types.ts` (zod schemas for Scale, Genre, Mood)
- [x] T3 Implement `src/domain/provenance.ts` (Provenance<T> type, source union, helper predicates)
- [x] T4 Implement `src/repository/errors.ts` (VocabularyLoadError, VocabularyIssue)
- [x] T5 Implement `src/repository/file-loader.ts` (private YAML discovery + parsing)
- [x] T6 Implement `src/repository/vocabulary-repository.ts` (public load/reload, maps, atomicity)
- [x] T7 Implement `src/factory/baseline.ts` (genre + mood -> clamped baselines)
- [x] T8 Implement `src/factory/resolution.ts` (per-field precedence chains)
- [x] T9 Implement `src/factory/validation.ts` (validation table -> ResolutionError with reason codes)
- [x] T10 Implement `src/factory/signature-factory.ts` (direct, intent, composite create())
- [x] T11 Implement `src/index.ts` (public re-exports)
- [x] T12 Tests: repository happy path (all 7 seed YAMLs load)
- [x] T13 Tests: repository failure cases (missing dir, bad YAML, schema violation, filename mismatch)
- [x] T14 Tests: factory direct composition (all fields = user provenance)
- [x] T15 Tests: factory tempo clamping with warnings
- [x] T16 Tests: factory unknown scale/genre/mood -> ResolutionError
- [x] T17 Tests: factory missing root_pitch_class -> ResolutionError
- [x] T18 Tests: factory intent composition with injected IntentResolver
- [x] T19 Tests: factory baseline correctness (phrygian + hypnotic-techno + arrakis with no user overrides)
- [x] T20 All 66 tests pass; build green; `pnpm status` renders M1 with both specs approved and impl_progress 100

## Done when

- All sub-tasks checked
- `pnpm install` succeeds at workspace root
- `pnpm --filter @stratum/vocabulary test` exits 0 (66 tests)
- `pnpm --filter @quicio/speck build && pnpm status` renders M1 with spec-0003 `impl_progress: 100` and the task marked `done`
- Single commit on `opencode/spec-0003-impl` branch (commit fcf4167, 25 files, 1750 insertions)
- Branch pushed to origin; ready to merge to main

## Notes

- OpenCode CLI timed out after writing all 22 files and running all 66 tests. The work was complete; only the final commit was not produced by the agent. Hermes (me) performed the final commit per Rule 9 (trivial, single-file task: commit the work).
- The 66 tests cover all 10 acceptance criteria from spec-0003.
- `pnpm build` and `pnpm typecheck` are green at the workspace root.

## Blocked / questions

- (none)

## References

- specs/0003 — Implementation Spec (the contract)
- specs/0001 — Domain Spec (authoritative behavior)
- specs/0002 — Implementation Spec (VocabularyRepository contract)
- vocabulary/ — 7 seed YAMLs
- docs/specs-hierarchy.md — Domain/Implementation split
